version: "3.9"
services:
  web:
    container_name: "mtb2020_web"
    build: .
    ports:
      - "3000:3000"
      - "8000:8000"
    environment:
      PORT: 8000
    volumes:
      - ../:/opt/mtb2020:cached
    command: /bin/sh -c "while sleep 1000; do :; done"
